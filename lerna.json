{
  "version": "0.0.1-alpha.0",
  "npmClient": "npm",
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "message": "publish %s"
    },
    "version": {
      "allowBranch": "master"
    }
  },
  "packages": [
    "packages/*"
  ]
}

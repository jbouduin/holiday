import { IFixedDate } from './fixed-date';
// import { Weekday } from './weekday';
// import { When } from './when';
// import { Which } from './which';

export interface IFixedWeekdayRelativeToFixed {
  // day: IFixedDate;
  // which: Which;
  // weekday: Weekday;
  // when: When;
}
